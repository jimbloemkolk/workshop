{
  "name": "@workshop/fetch-gitlab-failure",
  "version": "1.0.0",
  "type": "module",
  "description": "Fetch GitLab pipeline failure data — finds failed pipelines, downloads job logs, parses failure reasons",
  "main": "src/index.ts",
  "scripts": {
    "fetch": "tsx fetch-gitlab-failure.ts"
  },
  "dependencies": {
    "@workshop/gitlab-api": "workspace:*",
    "@workshop/fetcher-core": "workspace:*",
    "ink": "^6.5.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.19.2",
    "typescript": "~5.9.3"
  }
}
